mutation DeclineFriendRequest($params: FriendInput!) {
  declineFriendRequest(params: $params) {
    id
    nameId
    userId
    status
    friends {
      id
      nameId
      userId
      status
    }
    friendRequests {
      nameId
      userId
      status
    }
  }
}
