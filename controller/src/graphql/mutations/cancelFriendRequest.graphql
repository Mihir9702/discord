mutation CancelFriendRequest($params: FriendInput!) {
  cancelFriendRequest(params: $params) {
    id
    nameId
    userId
    status
    friends {
      id
      nameId
      userId
      status
    }
    friendRequests {
      nameId
      userId
      status
    }
  }
}
