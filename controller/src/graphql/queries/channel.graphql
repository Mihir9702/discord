query Channel($channelId: String!) {
  channel(channelId: $channelId) {
    name
    link
    serverId
    channels {
      name
      ptChat
      channelId
      server {
        serverId
      }
    }
  }
}
