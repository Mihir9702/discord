query Message($msgId: String!) {
  message(msgId: $msgId) {
    id
    msg
    msgId
    edited
    createdAt
    updatedAt
    user {
      id
      nameId
      userId
      iconId
    }
    channel {
      name
      channelId
      ptChat
    }
  }
}
