query UserFriends {
  userFriends {
    id
    nameId
    userId
    iconId
    friends {
      id
      nameId
      userId
      iconId
      status
    }
    friendRequests {
      nameId
      userId
      iconId
      status
    }
    blocked {
      id
      nameId
      userId
      iconId
      status
    }
  }
}
