import React from "react";
import Authentication from "src/components/Authentication";
import { Login } from "src/components/Authentication";

// g921

export default () => {
  return (
    <Authentication title="Login">
      <Login />
    </Authentication>
  );
};
